import React from 'react'
import '../../../../styles/styles.css'


const OversScreen = (props)=>(
    <div className="overs-screen">
         {props.children}
    </div>
);

export default OversScreen;